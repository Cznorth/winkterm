version: "3.9"

services:
  winkterm:
    # Set WINKTERM_IMAGE (e.g. ghcr.io/owner/winkterm:latest) on deploy to skip on-server builds.
    image: ${WINKTERM_IMAGE:-winkterm:local}
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
      - "8000:8000"
    environment:
      # --- LLM Provider ---
      # Use one of these:
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY:-}
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
      - OPENAI_BASE_URL=${OPENAI_BASE_URL:-}
      - MODEL_NAME=${MODEL_NAME:-claude-sonnet-4-20250514}
      # --- Optional services ---
      - PROMETHEUS_URL=${PROMETHEUS_URL:-http://localhost:9090}
      - LOKI_URL=${LOKI_URL:-http://localhost:3100}
      - DEBUG=${DEBUG:-false}
      - PYTHONPATH=/app
    volumes:
      - ./backend:/app/backend
      - winkterm-data:/root/.winkterm
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 10s
      timeout: 5s
      retries: 3

volumes:
  winkterm-data:
